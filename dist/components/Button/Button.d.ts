import React from 'react';
import type { ButtonProps } from './types';
declare const Button: ({ icon, label, handleClick, $variant, $size, disabled, transparent, outlined, cssProps }: ButtonProps) => React.JSX.Element;
export default Button;
