import React from 'react';
import type { ButtonProps } from './types';
declare const Button: ({ $icon, $iconPosition, label, handleClick, $variant, $size, disabled, $transparent, $outlined, $cssProps, ...props }: ButtonProps) => React.JSX.Element;
export default Button;
