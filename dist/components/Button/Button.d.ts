import React from 'react';
import type { ButtonProps } from './types';
declare const Button: ({ icon, label, handleClick, $variant, disabled, transparent, outlined }: ButtonProps) => React.JSX.Element;
export default Button;
