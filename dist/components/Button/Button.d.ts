import React from 'react';
import type { ButtonProps } from './types';
declare const Button: ({ icon, iconPossition, label, handleClick, $variant, $size, disabled, transparent, outlined, cssProps }: ButtonProps) => React.JSX.Element;
export default Button;
