import { ColorsType } from '../types/colors'

const palette = {
  navyBlue: 'rgba(15, 42, 60, 1)',
  white: 'rgba(255, 255, 255, 1)'
}

const COLORS: ColorsType = {
  primaryButton: {
    background: palette.navyBlue,
    color: palette.white
  }
}
export default COLORS
