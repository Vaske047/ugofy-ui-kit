import { SizeTypes } from '../types/layout'

const SIZE: SizeTypes = {
  borderRadius: {
    primaryButton: '6px'
  },
  padding: {
    primaryButton: '15px 25px',
    iconButton: '7px',
    smallButton: '10px'
  }
}

export default SIZE
